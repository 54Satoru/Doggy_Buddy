<% @reviews.each do |review| %>

  <%= review.user.username %>
  <%= image_tag review.user.image.url %>
  <%= review.position %>
  <div id="star-rate<%= review.id%>"></div>
  <script>
    $('#star-rate<%= review.id%>').raty({
      size      : 36,
      starOff   : '<%= asset_path('star-off.png') %>',
      starOn    : '<%= asset_path('star-on.png') %>',
      starHalf: '<%= asset_path('star-half.png') %>',
      half      : true,
      readOnly: true,
      score: <%= review.rate %>,
    });
    </script>
  <%= review.content %>

<% end %>

<% @review_sitters.each do |review| %>

  <%= review.user.username %>
  <%= image_tag review.user.image.url %>
  <%= review.position %>
  <div id="star-rate<%= review.id%>"></div>
  <script>
    $('#star-rate<%= review.id%>').raty({
      size      : 36,
      starOff   : '<%= asset_path('star-off.png') %>',
      starOn    : '<%= asset_path('star-on.png') %>',
      starHalf: '<%= asset_path('star-half.png') %>',
      half      : true,
      readOnly: true,
      score: <%= review.rate %>,
    });
    </script>
  <%= review.content %>

<% end %>

<% @review_cs.each do |review| %>

  <%= review.user.username %>
  <%= image_tag review.user.image.url %>
  <%= review.position %>
  <div id="star-rate<%= review.id%>"></div>
  <script>
    $('#star-rate<%= review.id%>').raty({
      size      : 36,
      starOff   : '<%= asset_path('star-off.png') %>',
      starOn    : '<%= asset_path('star-on.png') %>',
      starHalf: '<%= asset_path('star-half.png') %>',
      half      : true,
      readOnly: true,
      score: <%= review.rate %>,
    });
    </script>
  <%= review.content %>

<% end %>
